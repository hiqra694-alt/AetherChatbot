# FastAPI backend deps (Vercel serverless deployment -- see vercel.json).
# The voice worker (voice/agent.py) deploys separately to LiveKit Cloud and
# installs requirements-worker.txt instead, which layers its own
# LiveKit-specific packages on top of this same file -- see that file's
# header comment. Keep the two in sync: an import shared by both entry
# points (main.py and voice/agent.py) belongs here; anything only
# voice/agent.py's process needs (livekit-agents/plugins, psutil for its
# process-load calculator) belongs in requirements-worker.txt instead, so it
# never inflates the Vercel function bundle.
fastapi>=0.111.0
uvicorn>=0.30.1
google-generativeai>=0.7.2
openai>=2.0.0
anthropic==0.29.0
supabase
python-dotenv>=1.0.1
pytest>=8.2.2
pytest-asyncio>=0.23.7
httpx
pydantic>=2.11.0
pydantic-settings
beautifulsoup4
tzdata
PyMuPDF
voyageai
python-multipart
rank-bm25
fastembed
mcp==1.29.0
livekit-api==1.2.0
livekit-agents
